gate_id: M1-APP-GATE
milestone: M1
status: accepted
reviewer: artifact-reviewer
evidence_ids:
  - EVID-M1-ARTIFACT-SPIKE-003
  - EVID-M1-SQLITE-SPIKE-002
  - EVID-M1-APP-SPIKE-001
  - EVID-M0-CONTRACT-001
failed_samples: []
decision: >-
  Accepted on independent local E2 evidence for the bounded same-volume Windows artifact-staging spike only. QA/artifact-reviewer passed python test/contracts/m1-jobstore/test_artifact_staging_spike.py (8/8), python test/contracts/m1-jobstore/test_sqlite_jobstore_spike.py (5/5), node --test test/contracts/m1-jobstore/jobstore-spike.test.mjs (4/4), and npm run test:contracts:mcp-ocr (6/6). Task, handoff, and manifest validated against governance-v1; the manifest passed 6/6 workspace-relative path, existence, positive size, lowercase SHA-256, and self-exclusion checks. Staged scope contains only ADR, evidence, and test-spike files with zero production paths, and git diff --cached --check passed. This gate does not claim a production artifact publisher, persistent JobStore, ApplicationFacade, IPC, userData integration, recovery worker, or release behavior exists.
rollback: Delete only the M1-APP-003 ADR, evidence directory, and two artifact spike files, then restore the declared planning records. TemporaryDirectory removes generated bytes; no production artifact, database, user file, release object, immutable asset, channel pointer, or user data requires restoration.
next_action: Preserve this local E2 result as reversible spike evidence. Use a separately scoped, owned, and reviewed task for production integration, and obtain external evidence for power-loss durability, packaged Python 3.12, cross-volume or network filesystems, antivirus interference, real media, release, and canary behavior before making those claims.
timestamp: 2026-08-16T03:47:52Z
